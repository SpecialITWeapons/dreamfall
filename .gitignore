node_modules/
dist/
playwright-report/
test-results/
.vite/
*.local
.DS_Store
.superpowers/
.worktrees/

# the agent tooling's own directory: worktrees, scratch, local state
.claude/

# Baked by tools/figure/bake.py; committed only once the page loads it.
tools/figure/figure.glb

# Written by `npm run bench`: a measurement of one machine on one evening.
tools/bench/last.json

# Taken by `npm run parity:write`: photographs of one rasteriser, which is
# why they are not committed. The reason is in tools/parity/parity.config.ts.
tools/parity/references/
